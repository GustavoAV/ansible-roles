name: CI
on:
  # trunk-ignore(yamllint/empty-values)
  pull_request:
  push:
    branches: [main]

permissions: read-all

jobs:
  generate-matrix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the codebase
        uses: actions/checkout@v4
      
      - name: List Ansible roles
        id: set-matrix
        run: |
          echo "Finding roles in roles/..."
          roles=$(find roles -maxdepth 1 -mindepth 1 -type d -printf '%f,')
          # Trim trailing comma
          roles=${roles%,}
          # Construct JSON for matrix
          matrix=$(jq -n --arg rs "$roles" '{role: ($rs | split(","))}')
          echo "matrix=$matrix" >> $GITHUB_OUTPUT
    outputs:
      changed_roles: ${{ steps.set-matrix.outputs.matrix }}

  test:
    needs: generate-matrix
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}
    steps:
      - name: Checkout the codebase
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install Python packages
        run: |
          pipx install --include-deps ansible-dev-tools
          pipx inject --include-apps --include-deps ansible-dev-tools $(cat dev-requirements.txt)

      - name: Run Molecule tests
        run: |
          cd ${{ matrix.role }}/
          molecule test
